import { NextResponse } from 'next/server';

export async function GET() {
  return NextResponse.json({ message: 'API moved to frontend Firebase hooks' });
}

export async function POST() {
  return NextResponse.json({ message: 'API moved to frontend Firebase hooks' });
}
